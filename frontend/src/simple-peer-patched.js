// simple-peer-patched.js
// A wrapper around simple-peer to handle browser compatibility issues

import Peer from "simple-peer";

// Apply any patches or fixes here
// No additional modifications needed at the moment since we've added polyfills

export default Peer;
